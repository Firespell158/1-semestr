#include <iostream>

int main()
{
    // 1
    int n, sum = 0, maxi = INT_MIN, numb = 0;
    std::cin >> n;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        if (x < 0 and x % 7 == 0) {
            sum = sum + x;
            if (maxi < x) {
                maxi = x;
                numb = i;
            }
        }
        
        
    }
    std::cout << "sum = " << sum  << " " << "max = " << maxi << " " << "number is " << numb << std::endl;
    

    //2
    /*int n, tekn, pros = 1;
    std::cin >> n;
    while (n > 0) {
        tekn = n % 10;
        pros = pros * tekn;
        n = n / 10;
    }
    std::cout << pros << std::endl;
    */
}
